# 我实测了 119 个 MCP 服务器（55% 根本没法用）+ 110 个 Agent Skills —— 我做了一个"质检局"来告诉大家真相

> 项目：https://github.com/hyqzz/trustlens
> 排行榜（MCP 119 个 + Skills 110 个，可搜索/筛选/分页，每周自动更新）：https://trustlens.icodestar.net/

## 一、一个让人冒冷汗的现状

MCP（Model Context Protocol）发布不到两年，公共服务器已经超过 **6 万个**。但：

- 跨注册表安全研究显示 **66% 的服务器存在安全问题**
- **88% 的企业 Agent 试点死在上生产之前**——不是模型不行，是没人敢保证工具靠谱
- 你装任何一个 MCP Server 之前，没有任何地方能查到"它到底能不能用、安不安全"

这个生态有应用商店，但没有大众点评；有成千上万的商品，但没有质检局。

## 二、第一批大规模实测：55% 的服务器装完根本用不了

我从 npm / PyPI 注册表选取了 **122 个真实发布的 MCP 服务器**（官方参考实现 + 各来源的社区服务器，全部是真实存在的包），在**隔离环境、无任何 API 凭证**的条件下自动化评测。结果（119 个完成评测）：

| 等级 | 数量 | 占比 | 含义 |
|---|---|---|---|
| A | 3 | 3% | 真正开箱即用 |
| B | 11 | 9% | 可用，但有小问题 |
| C | 11 | 9% | 可用性一般 |
| D | 23 | 19% | 风险偏高 |
| **F** | **71** | **60%** | **装完无法使用** |

**超过一半的公共 MCP 服务器，按文档装完、不配任何额外凭证，根本无法启动或完成协议握手。** 失败原因归类（依据每个失败进程的 stderr 审计）：

- **运行时崩溃/代码错误（30 个）**：进程在握手窗口内直接崩退（JS/Python 栈、模块加载失败）
- **缺 API 凭证（15 个）**：文档没说，缺了 key 直接崩
- **包解析/兼容问题（11 个）**：`npx` 连可执行入口都找不到，或依赖装不上
- **真·挂起（9 个）**：进程起来但 90 秒内不响应初始化握手，很多是静默等待未文档化的配置或交互输入

还有 3 个（puppeteer、@enfyra、@mapbox）因会硬挂起评测进程，按方法论暂缓——浏览器自动化等类别需要按工具类型生成合理探针，这本身是下一个方法论课题。

**也有靠谱的**：A 级只有 3 个——`ifconfig-mcp`、`ref-tools-mcp`（并列榜首）与 `@siemens/element-mcp`；而广受欢迎的官方 `filesystem` 服务器只有 D 级（54.6）、网红 `desktop-commander` 也只有 D 级（48.0）——**"下载量大"和"能用"确实是两回事，"冷门"和"靠谱"也是两回事。**

## 三、TrustLens 是什么

一个开源的 Agent 能力生态信任基准，两套评测 + 一个界面：

1. **MCP 服务器质检流水线**：隔离环境部署 → 协议握手 → 静态安全扫描（提示注入/数据外发/硬编码凭证）→ 真实试调用 → **DeepSeek-V4 Flash 真实工具调用评测** → 0–100 信任分
2. **Agent Skills 评测**：对每个 SKILL.md 做结构质量 + 安全扫描 + 大模型可执行性实测——**已评测 110 个真实 Skill**（来自 GitHub 官方与社区仓库）
3. **公开排行榜**：中英双语，MCP 榜 + Skills 榜，支持搜索、筛选、排序、分页，每周自动更新

```bash
python -m trustlens check <server-name>
```

**所有评测数据以 JSON 提交在仓库里，git 历史即审计日志**——做信任生意，自己先得透明。

## 三·五、为什么要用真实模型测"工具调用"

排行榜上每个 MCP 服务器都有一项"模型工具调用"分——这不是静态估算，是让 **DeepSeek-V4 Flash 真实地读每个工具的定义，判断它会不会调用、能不能构造出合法参数**。生态里没人发布过这份数据：**同一个工具，模型到底能不能真正用起来。**

## 四、方法论（透明声明）

- 评测在**剥离环境变量的隔离进程**中执行，被测服务器**不拿到任何密钥**
- **开箱即用基准**：不填任何配置、不配任何 API key，按 `npx -y <pkg>` 标准方式启动，90 秒内完成握手才算"能启动"
- **防拖垮护栏**：同一服务器连续 2 次工具调用超时，即判定它对真实调用不可用，跳过剩余探针（否则单台病理服务器能拖垮整轮评测）
- 模型工具调用分由 **DeepSeek-V4 Flash**（性价比最高档）真实调用得出
- **安全维度的边界**：静态文本模式扫描，只抓提示注入/数据外发指令/硬编码凭证等明显特征，**无法检出运行时外发等隐蔽行为——"安全满分"≠"已证实安全"**。文中"66% 服务器有安全问题"是外部研究结论，与本榜评分口径不同
- Skills 评估覆盖结构完整性、安全扫描、以及模型可执行性判断
- 评测流水线每周自动复测，结果自动发布

## 五、接下来做什么（以及我需要你什么）

1. **能力可信**（进行中）：已接入 **DeepSeek-V4 Flash** 真实工具调用准确率（性价比最高档）；计划扩展到多模型对比——**国产模型的这个数据目前全世界没人发布**
2. **行为可信**：Agent 履约追踪与声誉分（对齐 W3C VC/DID、ERC-8004）
3. **交易可信**：Agent 经济的信用查询层

我需要的反馈：

- 你会在装 MCP 之前查评分吗？什么样的报告对你真正有用？
- 我们 90 秒握手超时的"开箱即用"标准，你觉得合理吗？
- 信任分四维度权重（功能 35/可靠 25/安全 25/兼容 15）该调整吗？

欢迎 star、issue、拍砖：https://github.com/hyqzz/trustlens

## 六、关于我

15 年软件工程师，现全职投入 AI 应用基础设施，来自智码星 ICodeStar。这个项目会长期做下去，评测数据每周自动更新。
